import 'package:flutter/material.dart';

class FutureBuildPrayerTime extends StatelessWidget {
  const FutureBuildPrayerTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
