import 'package:flutter/material.dart';

class CstmDiv extends StatelessWidget {
  const CstmDiv({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      thickness: 10.7,
    );
  }
}
