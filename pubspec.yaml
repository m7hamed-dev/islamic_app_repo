name: quran
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# build by specifying --build-name and --build-number, respectively.
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

# consider running `flutter pub upgrade --major-versions`. Alternatively,
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  # flutter_swiper_plus: ^2.0.4
  # state management
  provider: ^6.0.1
  # local storage
  shared_preferences: ^2.0.9
  # pdf
  # native_pdf_view: ^6.0.0+1
  flutter_cached_pdfview: ^0.4.1
  http: ^0.13.4
  # assets_audio_player: ^3.0.4+1
  # dotted_border: ^2.0.0+2
  hexcolor: ^2.0.5
  # percent_indicator: ^3.4.0
  sleek_circular_slider: ^2.0.1
  path_provider: ^2.0.8
  # shares and screenshots
  screenshot: ^1.2.3
  # share: ^2.0.4
  share_plus: ^3.0.4
  share_files_and_screenshot_widgets: ^1.0.6
  #
  flutter_svg: ^1.0.3
  # native spalsh screen
  flutter_native_splash: ^2.0.4
  # dates 
  hijri: ^3.0.0
  # qibah
  # flutter_compass: ^0.7.0
  # concentric_transition: ^1.0.1+1
  # lottie: ^1.2.2
  # dotted_border: ^2.0.0+2
  geolocator: ^8.2.0
  flutter_vibrate: ^1.3.0
  permission_handler: ^9.2.0
  # percent_indicator: ^4.0.0
  easy_localization: ^3.0.0
  sticky_headers: ^0.2.0
  api_cache_manager: ^1.0.2
  # quran: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # hive_generator: ^1.1.2
  # build_runner: ^2.1.8
  flutter_lints: ^1.0.0

#   # section change app icon
#   flutter_launcher_icons: "^0.9.2"
# flutter_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/logo.png"  
#  # end section change app icon

# flutter_native_splash:
#   image: assets/logo.png
#   color: "#ffffff"
  #background_image: "assets/background.png"

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/
    - assets/arabic_quran/
    - assets/json/
    - assets/pdf/
    - assets/praying_time/
    - assets/home/
    - assets/fonts/
    - assets/animation/
    - assets/translations/
    - assets/duaa_imgs/

  fonts:
    - family: pfd
      fonts:
        - asset: assets/fonts/PFDinTextUniversal-Medium.ttf
      # weight: 700

    - family: almarai
      fonts:
        - asset: assets/fonts/almarai.ttf
      # weight: 700
    
    - family: tajawal
      fonts:
        - asset: assets/fonts/tajawal.ttf
      # weight: 700   

    - family: cairo
      fonts:
        - asset: assets/fonts/cairo.ttf
      # weight: 700
   
    - family: DroidKufi
      fonts:
        - asset: assets/fonts/ArbFONTS-DroidKufi-Bold.ttf
      # weight: 700    
    
    - family: Hacen
      fonts:
        - asset: assets/fonts/Hacen Tunisia Regular.ttf    
    
    - family: dinn
      fonts:
        - asset: assets/fonts/DINNextLTArabic-Regular-3.ttf
    
    - family: dinn-reg
      fonts:
        - asset: assets/fonts/DINNextLTArabic-Regu.ttf
    
    - family: dinn-bold
      fonts:
        - asset: assets/fonts/ArbFONTS-DINNextLTArabic-Bold-4.ttf
      # weight: 700
